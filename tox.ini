[isort]
known_third_party=
    hangups,
    telepot,

[pytest]
cache_dir = .cache
addopts =
    --maxfail=1
log_level = DEBUG
log_format = %(asctime)s %(levelname)s %(name)s: %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
